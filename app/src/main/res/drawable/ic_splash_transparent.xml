<?xml version="1.0" encoding="utf-8"?>
<!-- Empty on purpose: satisfies windowSplashScreenAnimatedIcon's required attribute
     without showing a logo, since the OS icon safe-zone scaling clips/distorts our
     mark inconsistently across devices. The real animated logo is the Compose
     SplashOverlay shown immediately after. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="1dp"
    android:height="1dp"
    android:viewportWidth="1"
    android:viewportHeight="1" />
